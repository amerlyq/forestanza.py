# NOTE only one file for all langs, as they has the same groups!

# Main color to inherit, when omitted -- allows using its surrounding.
'': 15  # Used in term-only

Pronouns:
  '': [203, '#2233aa']  # USE by position
  highlight: '#aa3300'  # Override gui-only

Suffixes: 112

# Key dict, if need more variations -- BUT how then to flatten yaml?
# Auxiliary: {term: 33, gui: '#330011'}
